services:
  postgres:
    image: 'postgres:17'
    environment:
      - 'POSTGRES_DB=cityvoice'
      - 'POSTGRES_PASSWORD=${POSTGRES_PASSWORD}'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
